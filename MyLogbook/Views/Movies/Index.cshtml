@model IEnumerable<MyLogbook.Models.Movie>

@{
    ViewBag.Title = "Mes films";
}

<h2>Mes films</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
    @using (Html.BeginForm())
    {
    <p>
        <div class="form-inline">
            @Html.TextBox("SearchMovie", null, htmlAttributes: new { @class = "form-control" })
            <input type="submit" value="Filtrer" class="btn btn-default" />
        </div>
    </p>
    }
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Director)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cinema)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
@*
    <th>
        @Html.DisplayNameFor(model => model.UserId)
    </th>
*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Director)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cinema)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
@*
    <td>
        @Html.DisplayFor(modelItem => item.UserId)
    </td>
*@
        <td>
            <a href="@Url.Action("Edit", "Movies", new { id = item.Id })">
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
            </a>
            <a href="@Url.Action("Delete", "Movies", new { id = item.Id })">
                <span class="glyphicon glyphicon-trash" aria-hidden="true"></span>
            </a>
        </td>
    </tr>
}

</table>
